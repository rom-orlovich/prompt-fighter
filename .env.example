# Only needed for the (not yet implemented) live-model mode.
# The bundled demo transcripts require no key at all.
#
# Copy to .env and fill in. .env is gitignored — never commit a real key.
OPENROUTER_API_KEY=
